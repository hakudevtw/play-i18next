/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // i18n: i18n, // ! remove to prevent next config to redirect app pages to 404
};

module.exports = nextConfig;
